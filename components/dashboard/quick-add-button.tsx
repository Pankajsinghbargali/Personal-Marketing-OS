export function QuickAddButton() {
  return (
    <button
      className="fixed bottom-6 right-6 rounded-full bg-gold px-5 py-3 text-sm font-semibold text-black shadow-lg shadow-gold/30 transition hover:scale-105"
      type="button"
      aria-label="Quick add"
    >
      + Quick Add
    </button>
  );
}
